# הוראות מחייבות לסוכנים

מסמך זה הוא נקודת הכניסה לכל עבודה במאגר וחל על כל תיקיות המשנה.

## לפני שינוי

1. קראו את `docs/00-SSOT.md` ואת החוזים הרלוונטיים למשימה.
2. בדקו את הרכיבים והקוד הקיימים. אין ליצור חלופה לפני שנבדק שאין רכיב מתאים.
3. הגדירו אם השינוי הוא תוכן, עיצוב, תמונה, אייקון, רכיב או בדיקה.
4. החלטה נעולה נשמרת. סתירה או מידע חסר הם חסימה; אין להשלים מידע בהשערה.

## כללים שאינם ניתנים לעקיפה

- עברית ו־RTL בכל דף לומד. LTR מותר רק לרכיב טכני מקומי.
- דף לומד מכיל חומר למידה בלבד; לא תכנון שיעור, תסריט מורה או מידע ניהולי.
- מדריך מוצר נשען על תיעוד רשמי ועדכני של היצרן.
- צילום ממשק הוא צילום אמיתי של הגרסה הנלמדת. הדמיה אינה ראיה עובדתית.
- משתמשים במשתני העיצוב וברכיבים המשותפים; אין CSS מקומי שרירותי.
- אייקון ממשק מתאים למוצר ולגרסה; אייקון כללי מגיע מספריית SVG משותפת.
- אין לבנות CMS, מסד נתונים או תשתית חדשה בלי בקשה מפורשת.
- אין למחוק או לשכתב עבודה שאינה חלק מהמשימה.

## תהליך מחייב

1. מלאו `content/page-manifest.json` לפי `schemas/page-manifest.schema.json`.
2. אמתו מקורות וגרסה לפני כתיבת הוראות תפעוליות.
3. הרכיבו את הדף מבלוקים קיימים לפי `docs/03-COMPONENT-CONTRACT.md`.
4. שינוי גלובלי מתבצע פעם אחת ב־`assets/styles.css` או `assets/main.js`.
5. בצעו את הבדיקות שב־`docs/06-QA-CONTRACT.md`.
6. עדכנו סטטוס רק על סמך ראיות: `draft`, `review`, `verified`, `approved` או `blocked`.

## חסימת מסירה

מקור חסר, טענה לא מאומתת, צילום מומצא, RTL שבור, רכיב כפול, אינטראקציה כושלת או קריאות לקויה חוסמים `approved`. מציגים את הסטטוס האמיתי ואת הסיבה.

